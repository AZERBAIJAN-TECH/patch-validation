# patch-validation
used for patch validation in SGLoader

rejected patches usually used as malwares so they are blocked by default, approved are checked for their safeness and approved to use.

if patch is not approved or rejected, by default, you can load it, but if you set "Block" in patch safety level, it will not pass.
